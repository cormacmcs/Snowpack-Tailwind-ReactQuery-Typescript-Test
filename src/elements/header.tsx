import React from 'react';

export const Header = (props) => (
  <div
    className='container flex flex-row items-center justify-between w-screen max-w-none bg-red-500 font-bold text-white h-12 p-5'
    {...props}
  />
);
